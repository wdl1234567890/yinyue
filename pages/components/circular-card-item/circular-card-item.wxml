<view class="circular-card-container" style="width:{{size}}rpx">
  <circular-item src="{{cover}}" size="{{size}}" circular-item-class="cover-item"/>
  <text class="message">{{message}}</text>
</view>
